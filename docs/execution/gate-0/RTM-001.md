# RTM-001 — Initial Requirement Traceability Matrix

| الحقل | القيمة |
|---|---|
| Status | `IN PROGRESS` |
| Owner | Release Manager — named owner required |
| Plan | `FL-PMP-001 v1.1.0` |
| Updated | 2026-08-11 Asia/Baghdad |

الحالة `CONFIRMED OPEN` تعني أن الخطر مثبت في المصدر الحالي، وليس أنه نُشر أو عولج.

| ID | المتطلب | دليل المصدر الحالي | اختبار القبول الأدنى | الحالة |
|---|---|---|---|---|
| `PAY-P12-001` | Checkout ينشأ server-side وSuccess URL لا يمنح Credits؛ Webhook موثق فقط ينشئ Purchased Lot مرة واحدة | `packages/commerce` + `apps/engine-api/src/commerce` + Gate 12 pack | raw-body HMAC/timestamp، forged/stale rejection، delivery/event replay/conflict، exact amount/currency، shared-ledger grant and reset | `STAGE 12.1 PASS LOCAL — LEGAL/GATES 3,8 HOLD` |
| `SUB-P12-001` | Subscriber pins an immutable Plan Version; each exact renewal creates one expiring Subscription Lot and expiry never zeros Purchased Lots | Plan registry + Commerce subscriptions + shared whole-credit ledger | immutable ID/sequence, initial period required, plan drift/out-of-order rejection, renewal replay dedupe, purchased-lot survival | `STAGE 12.2 PASS LOCAL — LEGAL/GATES 3,8 HOLD` |
| `REV-P12-001` | Refund/chargeback append reversal evidence and withdraw only the original Lot's unused Credits without negative Wallet or history deletion | Commerce Invoice/Reversal records + Ledger `WITHDRAW_LOT` | exact amount/signature, replay/second-terminal rejection, net-value allocation, consumed shortfall receivable, chargeback fraud review | `STAGE 12.3 PASS LOCAL — LEGAL/ACCOUNTING HOLD` |
| `PROMO-P12-001` | Promotions require immutable Versions, funded dual budgets, server eligibility, caps, stacking/fraud controls and auditable subsidy lifecycle | Promotion engine + Quote/Operation integration + Subsidy Entries | exact hard-floor reserve, success redemption, expiry/failure release, budget/cap/fraud/stacking rejection, no hidden client eligibility | `STAGE 12.4 PASS LOCAL — LEGAL/FINANCE HOLD` |
| `RECON-P12-001` | Commerce must reconcile signed Events, Invoices, Credit Lots/Journals, reversals and Promotion subsidy evidence before a Gate decision | Commerce reconciliation report + real-socket HTTP E2E | exact chain reconstruction, 10000 bps/no issues, replay-safe payment, promoted settlement, full refund, explicit local PASS/formal HOLD | `STAGE 12.5 PASS LOCAL — FORMAL GATE 12 HOLD` |
| `SCORE-P13-001` | Profit routing must apply Hard Gates before an immutable exact score and must prevent hysteresis or stickiness from restoring an excluded Route | `packages/profit-router` + Gate 13 SCORE-001 evidence | 10000-bps policy validation, exact rational score, deterministic tie, hysteresis threshold, hashed TTL sticky override and zero Dispatch mutation | `STAGE 13.2 PASS LOCAL — SHADOW ONLY` |
| `SHADOW-P13-001` | Every Shadow routing decision must be explainable, tamper-evident and exactly replayable from versioned outcome metrics and immutable inputs | `packages/profit-router` + Gate 13 SHADOW-001 evidence | observation/operation dedupe, signature-scoped aggregates, minimum-sample readiness, hash chain, pre-append replay, projected quality/reliability report and zero Dispatch mutation | `STAGE 13.3 PASS LOCAL — SHADOW ONLY` |
| `CANARY-P13-001` | Exact auto-routing must advance only through approved cohorts and clean staged gates, with immediate safe rollback and one financial authority | `packages/profit-router` + Gate 13 CANARY-001 evidence | dual distinct approval, Shadow/replay readiness, deterministic Admin-first cohort, exact ladder, idempotent observations, margin/quality/SLO/cost gates, kill switch and no redispatch | `STAGE 13.4 PASS LOCAL — NO TRAFFIC ACTIVATED` |
| `GATE-P13-001` | Gate 13 must pass only with no Margin breach or quality/SLO regression and with every decision explainable/replayable | executable Gate evaluator + `GATE-13-EVIDENCE_DECISION` | local/formal decision separation, malformed evidence rejection, exact replay/ladder/cost/rollback criteria, explicit external blockers and no Production authorization on HOLD | `LOCAL PASS — FORMAL GATE 13 HOLD` |
| `OPTIN-P14-001` | Smart Mode must be explicit opt-in with disclosed automatic selection and the actual result Model, never a hidden Exact substitution | `packages/smart-beta` + Gate 14 OPTIN-001 evidence | four immutable Profiles, version-pinned consent, append-only opt-out, Plan eligibility, Exact denial, authorized candidate snapshot, actual Family/Model/Route disclosure and no Premium relabel | `STAGE 14.1 PASS LOCAL — NO SMART DISPATCH` |
| `EVAL-P14-001` | Smart satisfaction and quality must come from auditable structured Feedback and immutable Evaluation Policies, not silent weight learning | `packages/smart-beta` + Gate 14 EVAL-001 evidence | owner-only hashed feedback, idempotency/revisions, evaluator/policy pinning, exact 10000-bps math, signature isolation, sample readiness, evidence hash and zero routing/learning mutation | `STAGE 14.2 PASS LOCAL — FIXTURES ONLY` |
| `EXPLORE-P14-001` | Smart exploration must stay within 1–5%, be funded by a capped platform budget and never increase the customer contract | `packages/smart-beta` + Gate 14 EXPLORE-001 evidence | immutable rate/window/caps, consent/eval/profile gates, deterministic hash bucket, Margin Floor, BigInt incremental reserve/settle/release, user/global caps, hash ledger and instant Kill Switch | `STAGE 14.3 PASS LOCAL — NO EXPERIMENT ACTIVATED` |
| `EXPERIMENT-P14-001` | Smart experiments must publish transparent customer promises, use funded exploration and disclose every actual Model without hidden dispatch | `packages/smart-beta` + Gate 14 EXPERIMENT-001 evidence | immutable experiment policy, Draft/Final confirmation, bounded Variations, Relaxed SLA/no fake progress, reservation gate, output tuple evidence, threshold Kill Switch and pinned in-flight completion | `STAGE 14.4 PASS LOCAL — NO EXPERIMENT ACTIVATED` |
| `ADVISOR-P14-001` | CFO Advisor may read sanitized aggregates and propose/simulate, but can never publish, mutate Credits, top up a provider, activate a secret or delete a journal | `packages/smart-beta` + Gate 14 ADVISOR-001 evidence | immutable advisory Policy, sensitive-data rejection, exact Expected/P90/Maximum simulation, financial/Route signals, four Draft kinds, Maker handoff, hash chain and zero execution authority | `STAGE 14.5 PASS LOCAL — ADVISORY FIXTURES ONLY` |
| `GATE-P14-001` | Gate 14 passes only with no hidden substitution, budgeted experiments, instant rollback and satisfied Margin/satisfaction limits | executable Gate 14 evaluator + `GATE-14-EVIDENCE_DECISION` | bounded immutable evidence, disclosure/eval/budget/reconciliation/experiment/Advisor checks, zero Dispatch, formal dependency separation, evidence/decision hashes and fail-closed malformed input | `LOCAL PASS — FORMAL GATE 14 HOLD` |
| `OFFER-P15-001` | Unlimited may only mean a transparent Relaxed Draft offer with published Fair Use, shared queue/concurrency, restricted Routes and no hidden cap/substitution | `packages/unlimited-relaxed` + Gate 15 OFFER-001 evidence | immutable offer, honest allowance fallback, subscription/cohort/consent gates, hashed identity, bounded dimensions, actual Model disclosure, Premium Credit requirement and API/batch denial | `STAGE 15.1 PASS LOCAL — PILOT NOT ACTIVATED` |
| `BUDGET-P15-001` | Every included Unlimited Relaxed operation must reserve maximum COGS from a derived Cohort budget before spend and settle only verified Actual COGS | `packages/unlimited-relaxed` + Gate 15 BUDGET-001 evidence | BigInt value×ratio formula, matching Offer/Cohort/Authorization/Route gates, operation idempotency, aggregate/per-operation bounds, reserve/settle/release hash ledger and independent projection rebuild | `STAGE 15.2 PASS LOCAL — NO PILOT SPEND` |
| `RISK-P15-001` | Unlimited risk decisions must use representative P50/P90/P95/P99 plus price-shock and heavy-user scenarios, never average alone | `packages/unlimited-relaxed` + Gate 15 RISK-001 evidence | immutable nearest-rank Policy, sanitized unique user aggregates, reconciled budget pinning, 60-day/two-cycle readiness, exact BigInt quantiles, shocks, all-users-at-P99 scenario and no activation authority | `STAGE 15.3 PASS LOCAL — FIXTURE SIMULATION ONLY` |
| `CONTROL-P15-001` | An Unlimited Relaxed Pilot must start stopped, open only through separated approvals and support bounded Sales Stop plus immediate terminal Kill | `packages/unlimited-relaxed` + Gate 15 CONTROL-001 evidence | immutable pinned Policy, exact Risk/Budget evidence validation, Maker plus distinct Legal/Finance, published Cohort limit, budget/queue auto-stop, risk/reconciliation auto-kill, no redispatch and zero Production authority | `STAGE 15.4 PASS LOCAL — PILOT NOT ACTIVATED` |
| `GATE-P15-001` | Gate 15 passes only with Legal/Finance approval, no hidden cap and Cohort loss within the approved budget | executable Gate 15 evaluator + `GATE-15-EVIDENCE_DECISION` | bounded immutable evidence, offer disclosure, exact budget reconstruction, percentile/shock/heavy-user checks, loss limit, separated controls, formal dependency split, evidence/decision hashes and fail-closed malformed input | `LOCAL PASS — FORMAL GATE 15 HOLD` |
| `ROLLOUT-P16-001` | Release promotion must follow Internal Alpha, Invite Beta and 1→5→25→50→100 before GA, with no stage skip or release blocker | `packages/release-governance` + Gate 16 ROLLOUT-001 evidence | immutable release/artifact/Gate contract, four distinct approvals, minimum observation gates, Critical/High and financial stop, 50% Error Budget pause, hash audit and zero Production authority | `STAGE 16.1 PASS LOCAL — NO REAL TRAFFIC` |
| `DRILL-P16-001` | GA readiness requires passing Load, Soak, Chaos, Security and Restore drills with no security or financial release blocker | `packages/release-governance` + Gate 16 DRILL-001 evidence | exact scenario sets, load/concurrency/p95, soak window, failure injection, hostile security cases, RPO/RTO/rebuild/replay, sensitive-evidence rejection and aggregate readiness hash | `STAGE 16.2 PASS LOCAL — FIXTURE CONTRACT ONLY` |
| `OPS-P16-001` | Every release SLO and alert requires measurable ownership, Error Budget actions, drilled Runbook, escalation and truthful On-call coverage | `packages/release-governance` + Gate 16 OPS-001 evidence | exact SLO/P0/P1 inventory, SLI/data/window/owner/dashboard links, fast/slow burns, Kill Switch, P0/P1/P2 response, primary/backup separation and no live-readiness authority | `STAGE 16.3 PASS LOCAL — LIVE OPERATIONS NOT VERIFIED` |
| `GATE-P16-001` | Gate 16 local evaluation must consume source Policy/snapshot/report evidence, emit decision hashes, and never authorize Production | `packages/release-governance/src/gate.ts` + Gate 16 evidence decision | exact rollout/drill/operations/legacy linkage, incomplete source evidence rejected, local Formal=`HOLD` and Production=`DENIED` invariant | `LOCAL PASS — FORMAL GATE 16 HOLD` |
| `ROUTER-P13-001` | Profit routing excludes any candidate that fails a hard gate and compares exact expected cost per fully usable success | Profit Router foundation package + versioned Shadow policy | all gate reason fixtures, signature freshness, exact retry arithmetic, reliability-vs-nominal-cost golden cases, no dispatch authority | `STAGE 13.1 PASS LOCAL — SHADOW DATA HOLD` |
| `IMG-P10-001` | Image recipes publish explicit input, prompt, model and setting manifests | `src/features/creative-space/image-recipes.ts` + Gate 10 pack | five curated recipes; only certified local model exposed; defaults recover after refresh | `STAGE 10.1 PASS LOCAL — GATE 10 OPEN` |
| `IMG-P10-002` | Invalid bindings fail before Quote and recipe/model changes never drop data silently | `image-composer-validation.ts` + dynamic Inspector | missing/wrong/not-ready input, prompt/model/settings rejection; blocking/warning Compatibility Diff and explicit apply | `STAGE 10.2 PASS LOCAL — QUOTE/ENGINE VALIDATION PENDING` |
| `SPACE-P9-001` | Project domain graph is independent of the canvas rendering library | `src/features/creative-space/domain.ts` + `xyflow-adapter.ts` | storage contains domain positions/viewport and no React Flow truth; stable adapter identity | `IMPLEMENTED LOCALLY — GATE 9 NOT PASSED` |
| `SPACE-P9-002` | Bounded full-screen Standard workspace supports Quick Add, selection, Inspector, activity and viewer | `src/pages/CreativeSpacePage.tsx` | Desktop/browser keyboard and 390px touch-path inspection; no Console errors | `LOCAL UI PASS — A11Y/PERF/TUS PENDING` |
| `SPACE-P9-003` | Project position and viewport survive refresh and the adapter handles 100 cards | Creative Space storage/domain tests | exact refresh restore, bounded zoom rejection and 100 unique node projection | `LOCAL STORAGE PASS — SERVER PERSISTENCE PENDING` |
| `ADMIN-P8-001` | Sensitive Admin changes are command-based, versioned, audited and reversible by new version | `packages/admin-control-plane` + `apps/engine-api/src/admin-v2` + `docs/execution/gate-8` | strict lifecycle, idempotency conflict, immutable publish, compensating rollback and hash-chain tests | `IMPLEMENTED IN MEMORY — GATE 8 NOT PASSED` |
| `SEC-P8-001` | Admin requires AAL2, scoped RBAC and maker-checker; provider credentials are write-only | Admin V2 authorization and credential vault + local API | missing AAL2/role denied, same maker approval/activation denied, raw secret absent from all responses/audit | `LOCAL REFERENCE PASS — REAL IDP/KMS PENDING` |
| `ROUTE-P8-001` | Published route kill switch stops dispatch before provider spend and releases customer hold | Admin runtime guard + Local Provider integration | full API workflow then blocked dispatch; customer/provider balances unchanged | `PROVIDER FOR TEST ONLY — PERSISTENCE PENDING` |
| `FIN-P8-001` | Approved financial adjustments use the same append-only whole-credit ledger | Admin publish handler + ledger adjustment command | distinct maker/approver/publisher then exact wallet projection | `IMPLEMENTED LOCALLY — PRODUCTION GRANTS PENDING` |
| `MEDIA-P5-001` | Provider/user media is validated, scanned, checksummed and stored privately before delivery | `packages/media-pipeline` + `docs/execution/gate-5/MEDIA-001_PRIVATE_MEDIA_PIPELINE.md` | SSRF/private-IP, MIME/magic/polyglot, malware, size, quarantine and E2E tests | `IMPLEMENTED LOCALLY — GATE 5 NOT PASSED` |
| `MEDIA-P5-002` | Private access requires owner-bound short-lived grants | local private-store adapter + Engine asset route | missing/cross-owner/expired grant denied; anonymous Engine asset GET = 403 | `PASS LOCAL — STORAGE/RLS EVIDENCE PENDING` |
| `MEDIA-P5-003` | Resumable upload survives interruption without duplicate final object | `InMemoryResumableUploadService` | exact-offset resume, incomplete/checksum/cross-owner rejection, idempotent finalize | `PASS IN MEMORY — TUS/PERSISTENCE PENDING` |
| `ORCH-P6-001` | Quote confirmation atomically commits reserve, operation, idempotency binding and Outbox | `packages/durable-execution/src/atomic-operation.ts` + Engine integration | rollback-before/after writes; 100 repeated requests = one operation/reservation/event | `IMPLEMENTED IN MEMORY — DB TRANSACTION PENDING` |
| `ORCH-P6-002` | Queue and attempt state machines survive crash/redelivery without duplicate dispatch identity | `packages/durable-execution/src/outbox.ts` + `attempts.ts` | lease ownership, recovery, redelivery, dead-letter and illegal transition tests | `IMPLEMENTED IN MEMORY — MULTI-WORKER PENDING` |
| `ORCH-P6-003` | Unknown submission, callbacks, timeout/manual review and exact settlement fail closed | Engine local orchestration + Inbox + reconciliation | accepted-timeout lookup, callback duplicate/conflict, poll/lookup budget, provider loss and settlement E2E | `PROVIDER FOR TEST ONLY — KIE/GATE 6 NOT PASSED` |
| `TREAS-P7-001` | Funding Lots convert provider-native usage to auditable cash COGS including funding fees | `packages/provider-treasury/src/funding.ts` | FIFO/cumulative allocation, integer math, unknown/conflict/idempotency tests | `IMPLEMENTED IN MEMORY — FINANCE/DB EVIDENCE PENDING` |
| `TREAS-P7-002` | Shadow balance/runway covers commitments and spend controls gate dispatch | `packages/provider-treasury/src/treasury.ts` + Engine local integration | balance/commitment/burn/runway/reorder/circuit/limit tests and local dashboard | `PROVIDER FOR TEST REFERENCE — GATE 7 NOT PASSED` |
| `EQUIV-P7-001` | Cross-provider Exact requires an approved semantic/capability/quality evidence group | `packages/provider-treasury/src/equivalence.ts` | approved pair passes; same-name/unapproved/mismatched semantic hash fails | `HARD GATE LOCAL — NO APPROVED GROUPS` |
| `OR-P7-001` | OpenRouter async video, actual usage and raw-body HMAC are mapped without real spend | `packages/providers/src/openrouter-video-adapter.ts` + `openrouter-webhook.ts` | injected HTTP fixtures, microcredit usage.cost, unknown failure cost, signature/timestamp/raw-body tests | `FIXTURE PASS — OPENROUTER NOT CERTIFIED` |
| `ROUTE-P4-001` | Every route pins family/capability/billing/cost/adapter versions and certification evidence | `packages/commercial-engine` + `docs/execution/gate-4/ROUTE-001_PROVIDER_FOR_TEST.md` | reference, certification, capability, billing, kill-switch and E2E tests | `LOCAL TEST ROUTES ONLY — GATE 4 NOT PASSED` |
| `PRICE-P4-001` | Cost snapshots and customer prices are immutable and separate | `packages/commercial-engine/src/registry.ts` | overwrite rejected; expired/stale cost excluded; price-version change preserves old quote | `IMPLEMENTED LOCALLY` |
| `QUOTE-P4-001` | Deterministic whole-credit Quote pins all commercial versions and enforces hard margin floor | `packages/commercial-engine/src/quote-engine.ts` | golden billing, canonical hash, cost shock, hard floor, public leak tests | `IMPLEMENTED LOCALLY — NO PRODUCTION API` |
| `DATA-P3-001` | Logical ownership, immutable financial entities, uniqueness, tenant isolation and retention contract | `docs/execution/gate-3/DATA-001_LOGICAL_DATA_CONTRACT.md` | Domain invariants now; DB constraints/RLS/grants/concurrency after approved migration | `LOCAL DRAFT — DB EVIDENCE BLOCKED` |
| `FIN-P3-001` | Whole-credit append-only double-entry ledger with lots, reservations and projection rebuild | `packages/ledger` + `docs/execution/gate-3/FIN-001_WHOLE_CREDIT_LEDGER.md` | balance, non-negative, quote cap, lot conservation, 100-repeat idempotency, rebuild tests | `IMPLEMENTED LOCALLY — GATE 3 NOT PASSED` |
| `MIG-P3-001` | Expand/backfill/verify/cutover/contract migration workbook | `docs/execution/gate-3/MIG-001_DEFERRED_MIGRATION.md` | restore + dry run + counts/checksums + single writer + rollback evidence | `DEFERRED — NO MIGRATION AUTHORIZED` |
| `FOUND-P2-001` | Versioned executable API/Event/Transition foundation contracts | Plan sections 41 and 45; `docs/execution/gate-2` | OpenAPI policy tests + strict DTO leak tests + event schema tests + state transition CAS/authority/evidence tests | `TESTED LOCAL DRAFT — GATE 2 NOT PASSED` |
| `ENV-P0-001` | تدوير كل token ظهر خارج Secret Manager | tokens شاركت في المحادثة؛ لا تعاد كتابتها | old token rejected + new scoped identity works + audit evidence | `OPEN` |
| `SEC-P0-001` | Browser لا يقرر success/failure/refund/file URL | browser `generation-api` blocks non-local completion; `complete-generation` accepts only a fresh signed internal-workload HMAC and derives `user_id` from the stored reservation | JWT user cannot submit terminal state; verified Worker event only; payload actor is ignored | `LOCAL CODE HARDENING — DEPLOYED WORKER/EDGE EVIDENCE OPEN` |
| `SEC-P0-002` | Service Role ليس internal shared password | `start-generation` يوقّع طلبات `kie-ai`/`gemini-tts`، و`complete-generation` و`system-jobs` يقبلان Workload HMAC قصير العمر فقط؛ لا تستخدم هذه الوظائف `x-internal-caller` | service key header rejected; purpose HMAC/workload identity required | `LOCAL CODE HARDENING — SECRET/SCHEDULER/DEPLOYED EVIDENCE OPEN` |
| `OPS-P0-001` | reservation/idempotency لا يعيد Dispatch | local Engine commits reserve/operation/Outbox then dispatches through worker attempt | 100 same idempotency requests = 1 operation/reservation/event/attempt/task | `LOCAL REFERENCE PASS — LEGACY/DB CUTOVER OPEN` |
| `OPS-P0-002` | ambiguous provider timeout يصبح Submission Unknown | local Engine protects hold and resolves through provider idempotency lookup | accepted-then-timeout does not release or redispatch; bounded manual review | `LOCAL REFERENCE PASS — LEGACY/DB CUTOVER OPEN` |
| `SEC-P0-003` | lifecycle jobs server-owned | `use-generation-queue` لا يكتب `generation_jobs` أو `generations` بعد الآن؛ تحديث المتصفح display-only والعامل الموقّع يملك النهاية، وtest يحرس المسارات | authenticated JWT cannot INSERT/UPDATE/terminal transition | `LOCAL UI CONTAINMENT — DB POLICY/EDGE EVIDENCE OPEN` |
| `FIN-P0-001` | financial functions أقل صلاحية | legacy reserve/release/settle/cleanup functions | anon/auth EXECUTE denied; only scoped backend role succeeds | `OPEN — grants audit required` |
| `FIN-P0-002` | subscription expiry لا يصفر purchased wallet | `system-jobs` يحجب مسار `enforce_subscription_expiry` القديم إلى أن يثبت بديل ledger-aware؛ DB function/cron الخارجيان ما زالا غير متحققين | purchased legacy wallet survives; eligible subscription value only expires | `LOCAL COMPATIBILITY HOLD — DB/CRON EVIDENCE OPEN` |
| `FIN-P0-003` | لا time-only refund | `system-jobs` يحجب `cleanup_stale_reservations` التلقائي؛ لا إصدار رصيد عبر هذا المسار من دون دليل مزود/reconciliation | age alone cannot release; evidence/reconciliation required | `LOCAL COMPATIBILITY HOLD — DB/CRON EVIDENCE OPEN` |
| `SEC-P0-004` | user media private | Studio وAudio Studio لا يرفعان أو ينشئان public URL من المتصفح؛ Local Provider يستخدم مرجعاً محلياً فقط والتخزين النهائي مسؤولية عامل خادمي | anonymous GET/list fails; owner signed access passes | `LOCAL UI CONTAINMENT — STORAGE/RLS EVIDENCE OPEN` |
| `SEC-P0-005` | Admin لا يحذف evidence أو يكتب المال مباشرة | Admin Legacy لا ينفذ حذف Evidence أو RPC/كتابة مالية/حَوْكمة مباشرة؛ الأزرار تطلب أمر Admin خادمي موثق، وtest يمنع عودة هذا النمط | browser direct delete/financial mutation denied; command audited | `LOCAL UI CONTAINMENT — ADMIN V2 IDENTITY/PERSISTENCE/DB EVIDENCE OPEN` |
| `SEC-P0-006` | provider task lookup مربوط بمالك Operation | `kie-ai` يتحقق server-side من `generation_jobs.task_id + user_id` قبل كل Veo/Flux/standard status lookup | user A cannot query task/operation of user B | `LOCAL CODE HARDENING — ADVERSARIAL/DEPLOYED EVIDENCE OPEN` |
| `SEC-P0-007` | logs لا تحمل raw provider/user payload | shared `safe-edge-log` يقبل context scalar فقط؛ provider/completion/scheduler paths لا تسجل body أو URL أو prompt أو provider response/message خام | structured redaction tests pass; no prompt/URL/secret | `LOCAL CODE HARDENING — DEPLOYED LOG-SINK EVIDENCE OPEN` |
| `SEC-P0-008` | Edge function auth fail-closed | الخمس routes ذات `verify_jwt=false` مثبتة في test: `start-generation` JWT+HMAC-signing، `kie-ai`/`gemini-tts` JWT+HMAC للعمليات المدفوعة، و`complete-generation`/`system-jobs` Workload HMAC فقط | per-route manual/internal auth tests; anonymous denied where required | `LOCAL SOURCE CONTRACT PASS — DEPLOYED EDGE/SECRET EVIDENCE OPEN` |
| `DR-P0-001` | restore مثبت لا مفترض | لا يوجد restore report | timed restore + checksums + in-flight reconciliation report | `OPEN` |
| `GOV-P0-001` | named owners/backups/signoffs | `RACI-001` كله MISSING | كل دور حساس له إنسان وحساب عمل وبديل | `OPEN` |
| `CI-P1-001` | reproducible required checks | `.github/workflows/ci.yml` + `npm run ci:verify` | blocking typecheck/unit/build, Chromium E2E and Gitleaks; full lint remains an explicitly non-blocking baseline until existing debt is closed | `PARTIAL LOCAL — LINT/MIGRATION GATE OPEN` |
| `DEV-P0-001` | منع استهلاك رصيد المزود الرسمي أثناء التطوير المحلي | Provider Test API مستقل؛ local launcher/Engine يرفضان مفاتيح KIE/Gemini/OpenAI/OpenRouter/HMAC/Service Role وtokens الإدارية حتى لا تتصل بيئة Local بمزود حقيقي | separate Provider Test API + Bearer key + canonical Adapter/Registry + real HTTP request/poll/download/hash + dual-ledger reconciliation | `IMPLEMENTED LOCALLY — HTTP E2E PASS` |

## تحديث المصفوفة

أي PR أمني/مالي يذكر IDs المتأثرة، يضيف رابط test/evidence، ولا يحولها `PASS` بمجرد كتابة الكود. `PASS` يحتاج اختبارًا على بيئة معزولة ثم Gate approval.
